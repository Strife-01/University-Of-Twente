In order to run the code:

python3 ./bonus_queue_groupX.py

in order to modify it to check other things just call the function required at the end of the file and run the command from above in the terminal.
